apiVersion: v1
kind: Service
metadata:
  name: svc-api
  namespace: learning
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      # Service port defined for internal cluster access
      port: 80
      # Reference to API container port 80
      targetPort: 80
  type: ClusterIP
